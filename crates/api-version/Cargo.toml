[package]
name = "ferrlabs-api-version"
version = "1.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Date-based API contract versioning: negotiate a version per request and render older shapes to older clients"
readme = "README.md"
keywords = ["axum", "versioning", "api", "http", "middleware"]
categories = ["web-programming::http-server"]

[dependencies]
axum = { workspace = true }
http = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
tower = { workspace = true, features = ["util"] }

[lints]
workspace = true
