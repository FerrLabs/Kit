[package]
name = "ferrlabs-http"
version = "1.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Safe outbound HTTP client for FerrLabs APIs. SSRF defenses (IP allowlist, DNS pinning, manual redirect re-checks), bounded body and total deadline."

[dependencies]
reqwest = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
url = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
