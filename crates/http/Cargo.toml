[package]
name = "ferrlabs-http"
version = "2.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Outbound HTTP client with SSRF defenses: resolved-IP allowlist, DNS pinning, revalidated redirects, bounded body and total deadline"
readme = "README.md"
keywords = ["ssrf", "http", "security", "client", "reqwest"]
categories = ["web-programming::http-client", "network-programming"]

[dependencies]
reqwest = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
url = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
