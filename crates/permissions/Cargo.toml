[package]
name = "ferrlabs-permissions"
version = "2.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Typed scope and capability checks for FerrLabs APIs. Closed enum of every scope across the platform — adding one is a crate change, not a typo in a string literal."

[dependencies]
ferrlabs-errors = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
