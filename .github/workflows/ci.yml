name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    name: CI
    uses: FerrLabs/.github/.github/workflows/reusable-ci-rust.yml@5d33e5b2108483ee8769a57b1105a612fd8a9e67 # main
    with:
      enable-sonar: true
      apt-packages: build-essential pkg-config libssl-dev
      cargo-flags: --workspace --all-features
      coverage-flags: --all-features
      clippy-args: -D warnings
      enable-deny: false
      enable-typos: false
      enable-coverage: true
    secrets: inherit
